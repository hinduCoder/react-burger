import styles from './not-found.module.css';

const NotFoundPage = () => {
    return (
        <h1 className={`${styles.oops} text text_type_digits-large`}>404</h1>
    );
};

export default NotFoundPage;
